import { CheckRecentPlanEligibilityPipe } from './check-recent-plan-eligibility.pipe';

describe('CheckRecentPlanEligibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckRecentPlanEligibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
