import { CountComentPipe } from './count-coment.pipe';

describe('CountComentPipe', () => {
  it('create an instance', () => {
    const pipe = new CountComentPipe();
    expect(pipe).toBeTruthy();
  });
});
