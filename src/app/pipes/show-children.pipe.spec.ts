import { ShowChildrenPipe } from './show-children.pipe';

describe('ShowChildrenPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowChildrenPipe();
    expect(pipe).toBeTruthy();
  });
});
