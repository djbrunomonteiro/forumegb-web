import { ConvertSignalPipe } from './convert-signal.pipe';

describe('ConvertSignalPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSignalPipe();
    expect(pipe).toBeTruthy();
  });
});
